import { View, Text, StyleSheet } from "react-native";

import { DateTimePickerAndroid } from "@react-native-community/datetimepicker";

export default function DateInput({ label = "Fecha", value, onChange }) {
    return (
        
    )
}